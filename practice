//Prime Number
#include <stdio.h>

int main(void) {
	int i,n,temp,flag=0;
	scanf("%d",&n);
	temp = n/2;
	for(i=2;i<=temp;i++)
	{
		if(n%i==0)
		{
			flag=1;
			break;
		}
	}
	
	if(flag==0)
		printf("%d is a prime number",n);
	else
		printf("%d is not a prime number",n);
		
	return 0;
}

